export default class itemCarrinho {
  constructor(id, quantidade, valor, cc_for_unit) {
    this.id = id;
    this.quantidade = quantidade;
    this.valor = valor;
    this.cc_for_unit = cc_for_unit;
  }
}
